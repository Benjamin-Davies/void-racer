import React, { ReactElement } from 'react';
import './App.css';

import Viewport from './Viewport';

function App(): ReactElement {
  return (
    <div className="App">
      <Viewport />
    </div>
  );
}

export default App;
